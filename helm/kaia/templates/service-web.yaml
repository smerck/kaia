apiVersion: v1
kind: Service
metadata:
  name: kaia-web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: 80
  selector:
    app: kaia-web 