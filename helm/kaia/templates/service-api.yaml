apiVersion: v1
kind: Service
metadata:
  name: kaia-api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: 8080
  selector:
    app: kaia-api 